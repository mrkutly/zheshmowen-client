import React from 'react';
import { RouteComponentProps } from '@reach/router';
import Layout from './Layout';

const GroupsShow = (props: RouteComponentProps) => {
	return (
		<Layout>
			Show
		</Layout>
	);
};

export default GroupsShow;